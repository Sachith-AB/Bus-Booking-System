import 'package:flutter/cupertino.dart';

class OrderPlaceSuccess extends StatelessWidget {
  const OrderPlaceSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
